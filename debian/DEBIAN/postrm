#!/bin/sh
# postrm script for wkhtmltopdf
set -e

# Update library cache after removal
if [ "$1" = "remove" ] || [ "$1" = "purge" ]; then
    if [ -x /sbin/ldconfig ]; then
        /sbin/ldconfig
    fi
fi

exit 0
